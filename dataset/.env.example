# Copy this file to .env and replace the token with the value from
# Label Studio > Account & Settings > Access Token.
#
# This hostname is the Label Studio service name on the shared Compose network.
LABEL_STUDIO_HOST=http://label-studio:8080
LABEL_STUDIO_API_KEY=replace-with-your-label-studio-access-token
